module Lettrs where
{-import Html exposing (li, text, ul, img, div, h1, input)
import Html.Attributes exposing (class, src, type', accept, width, id, style)
-}

import Styles exposing (..)
import Text exposing (fromString, Text)
import Graphics.Element exposing (Element, show)

main : Element 
main =
  show (fromString "blah")
